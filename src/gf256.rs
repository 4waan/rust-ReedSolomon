fn generate_tables() -> ([u8; 256], [u8; 256]) {
    let mut g: [u8; 8] = [0, 0, 0, 0, 0, 0, 1, 0];
    let mut exp = [0u8; 256];
    let mut log = [0u8; 256];

    let mut k = 255;

    loop {

    }

    todo!()
}